---
galaxy_info:
  author: Ian Copp
  description: Install the latest stable youtube-dl via Homebrew.
  license: MIT
  min_ansible_version: 1.2
  platforms:
    - name: MacOSX
      versions:
        - all
  galaxy_tags:
    - system

dependencies:
  - role: icopp.homebrew
    when: ansible_os_family == "Darwin"

  - role: icopp.python
    when: ansible_os_family == "Darwin"
